<Page
    x:Class="_6.View.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_6.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="{x:Bind ViewModel.Theme}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" Margin="30">
            <TextBlock Text="{x:Bind ViewModel.Title}" FontSize="{x:Bind ViewModel.HeaderFontSize}"></TextBlock>
            <TextBlock Text="{x:Bind ContentString}" FontSize="{x:Bind ViewModel.ContentFontSize}"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
